server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Serve static files
    location / {
        try_files $uri $uri/ /index.html;
    }

    # No proxy needed - frontend calls backend directly via VITE_API_URL
    # The environment variable is baked into the build
}